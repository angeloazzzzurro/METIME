{
  "version": 2,
  "name": "metime-preview",
  "buildCommand": "mkdir -p .vercel/output/static && cp -r WebPreview/. .vercel/output/static/ && echo '{\"version\":3}' > .vercel/output/config.json",
  "outputDirectory": ".vercel/output"
}
